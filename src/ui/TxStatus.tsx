import Image from 'next/image'

const TxStatus = () => (
  <div className='flex items-center justify-center bg-transparent'>
    <Image src='/list.svg' alt='settings' width={25} height={25} />
  </div>
)

export default TxStatus